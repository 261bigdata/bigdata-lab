services:
  bigdata-lab:
    build: .
    restart: unless-stopped
    ports:
      - "8888:8888"
      - "4040:4040"
    volumes:
      - ../notebooks:/opt/notebooks
      - ../data:/opt/data
      - ../artifacts:/opt/artifacts
